pipeline {
    agent any
          stages {
            stage('Staging') {
                  steps {
                      sh 'sudo sh git.sh $ORIGIN $BRANCH
                      //sh 'sudo sh namespace.sh $NAME'

                  } }      }
}
