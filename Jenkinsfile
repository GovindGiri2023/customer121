pipeline {
    agent any
          stages {
            stage('Staging') {
                  steps {
                      sh 'git checkout -b $BRANCH'
                      //sh 'sudo sh namespace.sh $NAME'

                  } }      }
}
