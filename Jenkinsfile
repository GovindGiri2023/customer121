pipeline {
    agent any
          stages {
            stage('Staging') {
                  steps {
                      git checkout -b $BRANCH
                      //sh 'sudo sh namespace.sh $NAME'

                  } }      }
}
