pipeline {
    agent any
          stages {
            stage('Staging') {
                  steps {
                      sh 'git branch $BRANCH'
                      sh 'git branch -a'
                      //sh 'sudo sh namespace.sh $NAME'

                  } }      }
}
